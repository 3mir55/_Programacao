const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // muito usado para reconhecer números pares (0) e ímpares (n)

console.log(soma, subtracao, multiplicacao, divisao, modulo)
