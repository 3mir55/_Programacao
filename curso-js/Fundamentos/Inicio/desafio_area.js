// PI = 3.141592 // por convenção letra maiúscula por ser uma constante
// Pi*R² = PI * raio * raio

const PI = 3.141592;
let raio = 10;
let areaCirc = PI * raio * raio;
console.log("A área é: " + areaCirc + " m².");
console.log(Math.PI);
areaCirc = Math.PI * raio * raio;
console.log(areaCirc);
